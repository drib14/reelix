# ===================================================
# Render Infrastructure as Code Blueprint Specification
# Project: Reelix Fullstack (Frontend & Backend API)
# ===================================================

services:
  # ---------------------------------------------------
  # 1. Backend Web Service (Node.js Express API)
  # ---------------------------------------------------
  - type: web
    name: reelix-api
    runtime: node
    plan: free
    region: singapore
    buildCommand: npm install --no-audit --no-fund --loglevel=error
    startCommand: node backend/index.js
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: 10000
      - key: NODE_ENV
        value: production
      - key: MONGO_URI
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: TMDB_API_KEY
        sync: false
      - key: TMDB_READ_ACCESS_TOKEN
        sync: false
      - key: GROQ_API_KEY
        sync: false
      - key: GROQ_BASE_URL
        value: https://api.groq.com/openai/v1
      - key: GROQ_MODEL
        value: llama-3.3-70b-versatile
      - key: CLIENT_URL
        sync: false

  # ---------------------------------------------------
  # 2. Frontend Static Site (Vite React Web App)
  # Service Name: reelix (if claimed, rename to reelix-web)
  # ---------------------------------------------------
  - type: web
    name: reelix
    runtime: static
    buildCommand: cd frontend && npm install --no-audit --no-fund --loglevel=error && npm run build
    staticPublishPath: ./frontend/dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_BASE_URL
        fromService:
          type: web
          name: reelix-api
          property: host
